# Ollama Game Automation

This directory contains an example script that uses Puppeteer to control a browser-based game.

## Usage

1. Install dependencies (requires Node.js)
   ```bash
   npm install puppeteer
   ```
2. Run the bot script
   ```bash
   node gameBot.js
   ```

You can copy this folder to a new repository if you want to maintain it separately.
